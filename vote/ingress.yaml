apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: voting-app-vote-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: voting-app-vote-service
                port:
                  number: 8080
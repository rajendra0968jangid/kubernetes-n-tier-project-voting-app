apiVersion: v1
kind: Service
metadata:
  labels:
    app: result
  name: voting-app-result-service
spec:
  type: NodePort
  selector:
    app: result
  ports:
    - port: 8081
      targetPort: 80
      nodePort: 31001
      name: "result-service"
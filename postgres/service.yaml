apiVersion: v1
kind: Service
metadata:
  labels:
    app: db
  name: voting-app-db-service
spec:
  type: ClusterIP
  ports:
    - name: "db-service"
      port: 5432
      targetPort: 5432
  selector:
    app: db
